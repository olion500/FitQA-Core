spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/fitqa_db
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: create
  profiles:
    include: oauth

server:
  port: 8080
